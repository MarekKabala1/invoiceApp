import { Text, View } from 'react-native';

export default function Index() {
	return (
		<View className='flex-1 items-center justify-center bg-slate-500'>
			<Text>Hello in the Invoice App</Text>
		</View>
	);
}
